<?xml version="1.0"?>
<configuration>
  <atom type="Attributes">
    <hash type="Sheet" key="red_layout_switcher_bottom:sheet">
      <atom type="Label">_RED_Layout_Switcher_Bottom</atom>
      <atom type="Style">pie</atom>
      <atom type="IconMode">both</atom>
      <atom type="EditorColor">65536</atom>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabAVPTag}">
        <atom type="Label">Model</atom>
        <atom type="Tooltip">Model Viewport

Shows or hides a Model viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.model</atom>
      </list>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabIVPTag}">
        <atom type="Label">Scripting</atom>
        <atom type="Tooltip">Scripting Viewport

Shows or hides a Scripting viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.scripting</atom>
      </list>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabGVPTag}">
        <atom type="Label">Graph Editor</atom>
        <atom type="Tooltip">Graph Editor Viewport

Shows or hides a Graph Editor viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.graph</atom>
      </list>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabFVPTag}">
        <atom type="Label">Schematic</atom>
        <atom type="Tooltip">Schematic Viewport

Shows or hides a Schematic viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.schematic</atom>
      </list>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabEVPTag}">
        <atom type="Label">Presets</atom>
        <atom type="Tooltip">Preset Browser Viewport

Shows or hides a Preset Browser viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.presets</atom>
      </list>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabCVPTag}">
        <atom type="Label">Scene</atom>
        <atom type="Tooltip">Scene Viewport

Shows or hides a Scene viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.scene</atom>
      </list>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabDVPTag}">
        <atom type="Label">Camera</atom>
        <atom type="Tooltip">Camera Viewport

Shows or hides a Camera viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.camera</atom>
      </list>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabBVPTag}">
        <atom type="Label">UV</atom>
        <atom type="Tooltip">UV Viewport

Shows or hides a UV viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.uv</atom>
      </list>
      <list type="Control" val="cmd viewport.hide ? tag {LayoutMODOXXCentreLowerPanelVPTag} down {LayoutMODOXXCentreLowerTabVPKey} {LayoutMODOXXCentreLowerTabHVPTag}">
        <atom type="Label">Sculpt &amp; Paint</atom>
        <atom type="Tooltip">Sculpt &amp; Paint Viewport

Shows or hides a Sculpt &amp; Paint viewport below the main viewport.
Clicking on the button when it's active will hide the viewport.</atom>
      <atom type="IconResource">switcher.expand.brush</atom>
      </list>
    </hash>
  </atom>
</configuration>
